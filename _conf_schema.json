{
  "whitelist": {
    "type": "list",
    "description": "启用插件的会话 ID 白名单",
    "default": [],
    "hint": "每个元素为一个会话 ID，例如 QQ 号（留空表示全部启用）"
  },
  "timezone": {
    "type": "string",
    "description": "时区",
    "hint": "例如 Asia/Shanghai",
    "default": "Asia/Shanghai"
  },
  "sleep_hours": {
    "type": "string",
    "description": "睡眠时段",
    "hint": "格式 HH:MM-HH:MM，例如 23:00-07:00，支持跨天",
    "default": "23:00-07:00"
  },
  "max_triggers": {
    "type": "int",
    "description": "单个会话最大触发器个数",
    "hint": "若值被减小，超出时最早创建的触发器会被丢弃",
    "default": 5
  },
  "decision_prompt": {
    "type": "text",
    "description": "决策 AI 的系统提示词",
    "hint": "用于告诉 AI 如何判断是否创建触发器",
    "default": "你是一个主动闲聊决策助手。根据当前对话上下文和用户沉默时间，判断是否需要在未来某个时间点主动发起聊天。如果决定发起，请使用本插件的触发器管理工具创建触发器。\n重要：本插件的 create_trigger 仅用于创建会被用户消息清空的临时闲聊触发器。\n如需创建持久定时任务（闹钟、提醒等），必须使用系统内置的 future_task 工具。\n请务必根据任务性质选择正确的工具。"
  },
  "decision_max_history_messages": {
    "type": "int",
    "description": "超时决策时提供给 AI 的最大历史消息条数",
    "default": 20
  },
  "decision_timeout_seconds": {
    "type": "int",
    "description": "AI 回复后等待用户回复的超时时间（秒）",
    "hint": "超时后触发决策",
    "default": 300
  },
  "daily_analysis_times": {
    "type": "string",
    "description": "每日分析时间点",
    "hint": "多个用逗号分隔，格式 HH:MM",
    "default": "07:00,16:00"
  },
  "daily_analysis_prompt": {
    "type": "text",
    "description": "每日分析 AI 的系统提示词",
    "default": "你是一个对话分析助手。分析最近的历史对话，判断是否需要在未来主动发起闲聊。如果认为合适，使用触发器管理工具创建触发器，指定合适的触发时间、额外提示词等。"
  },
  "daily_analysis_max_history_messages": {
    "type": "int",
    "description": "每日分析时提供给 AI 的最大历史消息条数",
    "default": 50
  },
  "inactive_threshold_hours": {
    "type": "int",
    "description": "用户在多少小时内无任何消息时，停用每日分析功能",
    "hint": "（设为 0 表示永不停止）",
    "default": 24
  },
  "inject_date_tip": {
    "type": "bool",
    "description": "是否在决策/分析提示词中注入当前日期和节假日信息",
    "default": true
  },
  "trigger_persist": {
    "type": "bool",
    "description": "是否持久化保存触发器队列（重启后恢复）",
    "default": true
  }
}
